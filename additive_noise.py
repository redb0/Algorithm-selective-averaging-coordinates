
def get_amplitude(f_index):
    # ?????
    if f_index == 1:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a
    # ?????
    if f_index == 2:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a
    # ?????
    if f_index == 3:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a

    if f_index == 4:
        real_min = -1.2
        real_max = 0
        a = real_max - real_min
        return a

    if f_index == 5:
        real_min = 0
        real_max = 27
        a = real_max - real_min
        return a

    # ?????
    if f_index == 6:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a

    if f_index == 7:
        real_min = 0
        real_max = 80
        a = real_max - real_min
        return a

    if f_index == 8:
        real_min = 0
        real_max = 8.3
        a = real_max - real_min
        return a

    if f_index == 9:
        real_min = 0
        real_max = 2.2
        a = real_max - real_min
        return a

    if f_index == 10:
        real_min = 1
        real_max = 500
        a = real_max - real_min
        return a

    if f_index == 11:
        real_min = 0
        real_max = 21
        a = real_max - real_min
        return a

    if f_index == 12:
        real_min = -18
        real_max = 15
        a = real_max - real_min
        return a

    if f_index == 13:
        real_min = 0
        real_max = 20
        a = real_max - real_min
        return a

    if f_index == 14:
        real_min = 0
        real_max = 19
        a = real_max - real_min
        return a

    if f_index == 15:
        real_min = 0
        real_max = 55
        a = real_max - real_min
        return a

    if f_index == 16:
        real_min = 0
        real_max = 27
        a = real_max - real_min
        return a
    # ?????
    if f_index == 17:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a
    # ?????
    if f_index == 18:
        real_min = 0
        real_max = 0
        a = real_max - real_min
        return a

    if f_index == 19:
        real_min = 0
        real_max = 15
        a = real_max - real_min
        return a

    raise Exception("Введен неверный индекс тестовой функции")